const REFRESH_DIRECT_LINE_TOKEN = 'REFRESH_DIRECT_LINE_TOKEN';

export default function refreshDirectLineToken() {
  return { type: REFRESH_DIRECT_LINE_TOKEN };
}

export { REFRESH_DIRECT_LINE_TOKEN }
